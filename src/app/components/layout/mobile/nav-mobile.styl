.nav-mobile {
  padding-top: 60px;

  .nav-header {
    top: 0;
    left 0;
    position fixed;
    box-shadow: 0px 0px 40px 0px rgba(82, 63, 105, 0.1);
    width 100%;
  }

  .app-route {
    padding 15px;
  }
}